import { Heart } from "lucide-react";
import { ComingSoon } from "@/components/ComingSoon";

const Wishlist = () => (
  <ComingSoon
    title="Wishlist & Trades"
    icon={Heart}
    description="Track cards you want, list duplicates to trade, and compare values instantly."
  />
);
export default Wishlist;
